# 阶段工作总结（2026-03）

## 总体结论

当前项目主链路可稳定运行，P0/P1 已完成，P3 中增量更新与多出行方式已完成，完美见证优化为部分完成（可选启用、失败自动降级）。

## 完成情况总览

| 方向 | 目标 | 状态 | 说明 |
|---|---|---|---|
| P0 | 路径详情展示 | 已完成 | 展示距离、时间、基础时间、规则度量时间、途经点统计 |
| P0 | CCH 索引常驻 | 已完成 | 路由服务常驻，支持重载流程 |
| P1 | 缓存策略 | 已完成 | 路由链路接入 Redis + 内存后备缓存，支持 flush |
| P1 | 车牌限行扩展 | 已完成 | 支持尾号、星期、时段策略；命中/未命中分流正确 |
| P3 | 增量更新 | 已完成 | 提供 update_weights 接口，支持局部弧权更新 |
| P3 | 多出行方式 | 已完成 | 支持 walking/bus，前后端联通 |
| P3 | 完美见证优化 | 部分完成 | 已接入开关与校验，失败写状态并自动降级 |

## 本阶段关键改动

### 后端（C++）

- 路由返回新增字段: metric_travel_time, base_travel_time
- RESOLVE_POLY 策略为 inside_or_intersect
- 支持 walking/bus profile
- 新增 UPDATE_ARCS 命令
- 完美见证流程增加:
  - 构建后校验
  - 状态文件记录
  - 失败后跳过重试（可强制重建）

### 网关（Node）

- 路由缓存: Redis + 内存后备
- 新增 POST /api/cache/flush
- 新增 POST /api/graph/update_weights
- route 支持 plate 与 query_date
- 车牌策略控制规则 metric 是否生效

### 前端（React）

- profile 扩展: walking / bus
- 路由查询扩展输入: plate / queryDate
- 路径详情增强: 规则度量时间与基础时间并列展示
- RulePanel 支持 plate_policy 配置

## 已完成验证

- 前端构建通过（npm run build）
- 路由服务可稳定启动
- 二次启动可观察到完美见证失败冷却跳过逻辑
- 一键验收脚本 tools/acceptance_p0_p1_p3.sh 全部通过

## 当前边界与风险

- 完美见证在当前数据集上的构建稳定性不足，当前采取自动降级保障可用性。
- 仍需补充长期自动化回归（建议将验收脚本接入 CI）。

## 建议下一步

1. 增加完美见证状态查询接口，便于可观测性与运维。
2. 将验收脚本接入 CI，形成每次提交自动验证。
3. 持续推进 P2（多目标路径与历史收藏）以补齐产品能力。
